{
  "name": "illyriad-sov-scanner",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "description": "Userscript ranking Illyriad world-map tiles by sustainable tax rate.",
  "scripts": {
    "build": "node build.mjs",
    "release": "node build.mjs --release",
    "watch": "node build.mjs --watch",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
